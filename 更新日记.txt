版本1.0 
服务端：增加了上传客户端的提醒，每次发送1000单位，并在发送的过程中显示。
客户端：客户端一直运行，等待服务端来连接，就算服务端退出，客户端也不会退出。服务端可以随时链接客户端，只要客户端没有退出。

版本2.1
服务端和客户端都加入了发送和接受验证。保证发送和接受正常进行。若三次发送或者三次接受失败，跳出发送货接受函数。

版本2.2
客户端在服务端退出之后增加了释放socket操作